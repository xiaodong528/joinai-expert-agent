# Refinery Spec Review Note

此文件保留给旧引用兼容。

在 `software-prototyper` 中，不再使用“通用规格审查子任务”语义。
如果流程引用到这里，应按以下 GT 语义解释：

- 审查者是 `Refinery`
- 审查输入是 `Polecat` 提交的 bead 产物与冻结规格
- 审查动作必须基于代码、测试和正式证据路径
- 结论必须输出 `pass / warn / fail`，并驱动 `Mayor` 下一步动作

不要把这里解释成通用审查子任务模板。
