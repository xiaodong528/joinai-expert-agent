# Polecat Bead Execution Note

此文件保留给旧引用兼容。

在 `software-prototyper` 中，不再使用“通用实现者子任务”语义。
如果有文档或流程引用到这里，应按以下 GT 语义解释：

- 执行者是 `Polecat`
- 任务单元是 GT bead
- 输入来自 `Mayor` 冻结后的计划与 bead 描述
- 输出是代码、测试结果、日志与正式证据路径
- 完成后提交给 `Refinery` 独立验收

不要把这里解释成通用任务分发模板、同会话实现者模板或外部通用子任务模板。
